using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OptionsCanvas : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print("I am Options Cnavas");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
